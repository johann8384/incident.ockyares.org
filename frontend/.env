# React Environment Variables
# All services run behind nginx on the same domain with different paths

# PostgREST API endpoint (behind nginx at /api/)
REACT_APP_API_URL=/api

# Flask Backend endpoint (behind nginx at /)
REACT_APP_BACKEND_URL=

# For production with SSL:
# REACT_APP_API_URL=https://incident.ockyares.org/api
# REACT_APP_BACKEND_URL=https://incident.ockyares.org

# For local development with nginx proxy:
# REACT_APP_API_URL=http://localhost/api
# REACT_APP_BACKEND_URL=http://localhost
